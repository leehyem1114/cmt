






<from action="/sales/so" method="get" th:object="${SaleOrder}">
			<div class="container">
				<label>회사</label>
				<select th:filed="*{cltNo}">
					<option th:each="${ctlModel}"
						th:value="${ctlNo}"
						th:text="${ctlName}">
					</option>
				</select>
			</div>
			<div class="container">
				2
			</div>
			<button type="button">전송</button>
		</from>








<form th:object="${user}">
    <select th:field="*{type}">
        <option th:each="option : ${types}" 
                th:value="${option}" 
                th:text="${option}" 
                th:selected="${user.type == option}"></option>
    </select>
    <button type="submit">Submit</button>
</form>